import React from 'react';

const FreelancerSingleService = ({myservice}) => {
    const {title} = myservice;

    return (
        <div>
            <h2>{title}</h2>
            
        </div>
    );
};

export default FreelancerSingleService;